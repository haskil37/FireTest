@model FireTest.Models.TeacherFinishTest
@{
    ViewBag.Title = "Удалить итоговое тестирование";
}
<div class="row">
    <h3>Действительно хотите удалить итоговый тест под названием "@Model.NameTest"?</h3>
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="row center">
        <input type="submit" value="Удалить" class="button" />
    </div>
    <div class="row center">
        @Html.ActionLink("На главную", "Index", null, new { @class = "button-Cancel" })
    </div>
}